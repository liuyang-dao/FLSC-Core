# FLSC-META-V1.1 补丁建议

> **适用文档**：
> - FLSC-SMT-ORC-5LAYER-V1.0（五层↔五级镜像标尺）
> - FLSC-CAPTURE-STRUCT-DAO-V1.0（结构捕捉 vs 道捕捉分工）
>
> **补丁编号**：B-01 ~ B-06
> **目标**：修复两份元架构文档的六处断裂面，MIS_true 预期 0.90→0.93

---

## B-01：K↔ORC5 反推半页证明

### 问题（来自评价）

文档一 §4 表里 K 是"终极约束（道觉守恒）"，但把"主客二分"预设归属 K 层的论证缺失——只给结论，没展开推导。

### 补丁内容

在 §3 ORC5 段落后补半页论证：

> **K 层为何承载主客二分预设？**
>
> K 层本体定义为"全局守恒与边界刚性规则"。在五层架构中，K 是**唯一同时约束内部动态（守恒律）和外部边界（刚性规则）的层**——它既是"道觉守恒"的正向表述，也是"主客分裂"的反向根源。
>
> 当道觉元一通过 K 层显化为"守恒规则"时，隐含了一个操作前提：**必须有一个"观测者"去确认守恒、一个"被观测者"去遵守守恒**。守恒律的成立，预设了"测量者 vs 被测量者"的二元结构。
>
> 因此消解 K 层 = 消解"谁在守恒/谁在测量"的二元预设 = 回归道觉不二。
>
> **这正是 V5.0 公理 A5「本觉自明」的操作论对应：觉明无需观测者，守恒无需测量者。**

### 影响脊线

- ORC-SPINE-05（刚性边界脊）→ PARTIAL → CLOSED
- ORC-SPINE-03（镜像同构脊）→ 强化论证链

---

## B-02：跨阶跳跃判定标准

### 问题（来自评价）

文档一 L-02 承认"真实跳跃可跨阶、退转、同层徘徊"，但没给判定标准——什么算跨阶？残余如何折算？

### 补丁内容

在 §5.2 后新增 §5.3「跨阶跳跃判定算法」：

```python
def detect_cross_jump(orc_history: list[int]) -> dict:
    """
    判定跳跃序列是否含跨阶/退转/同层循环
    
    输入: orc_history = [1,3,2,4,5] 等跳跃阶数序列
    输出: {cross_jump: bool, regression: bool, loop: bool, residual_carry: float}
    """
    result = {
        'cross_jump': False,   # 跨阶（如1→3跳过2）
        'regression': False,   # 退转（如3→2）
        'loop': False,         # 同层循环
        'residual_carry': 0.0  # 跨阶携带残余量
    }
    
    for i in range(1, len(orc_history)):
        diff = orc_history[i] - orc_history[i-1]
        
        if diff > 1:
            result['cross_jump'] = True
            # 跨阶残余 = 跳过阶数的残余之和
            skipped = sum(get_residual(j) for j in range(orc_history[i-1]+1, orc_history[i]))
            result['residual_carry'] += skipped
            
        elif diff < 0:
            result['regression'] = True
            
        elif diff == 0:
            result['loop'] = True
    
    return result

def get_residual(orc_level: int) -> float:
    """各ORC阶对应的标准残余量（需实证校准）"""
    residuals = {1: 0.15, 2: 0.20, 3: 0.18, 4: 0.22, 5: 0.10}
    return residuals.get(orc_level, 0.0)
```

### 跨阶合法性判定表

| 情形 | 残余携带 | 合法性 | 处置 |
|------|---------|--------|------|
| 跨阶 1→3，残余折算≥0.35 | 高 | 需补 ORC2 回溯 | 不允许跳过 |
| 跨阶 2→4，残余折算<0.20 | 低 | 可记录为合法跨阶 | 标注 B-02 |
| 退转 4→3 | — | 合法（认知回落） | 记录为 regression |
| 同层循环 ≥3 次 | — | 警示 | 触发 L4_FROZEN 审查 |

### 影响脊线

- ORC-SPINE-02（五级递归脊）→ 补判定标准
- 新增 L-06 诚实补丁："跨阶跳跃需残余折算，不允许跳过含高残余的阶数"

---

## B-03：Step3 残余熔断触发标准

### 问题（来自评价）

文档二 Step3「AI 反推数学」遇不可形式化残余时该停该问？没写。

### 补丁内容

在 §四 Step3 后新增熔断条款：

```yaml
Step3_residual_fuse:
  trigger_conditions:
    - "遇觉明度不可量化(F41类残余)"
    - "遇主客不二不可公式化(A5类约束)"
    - "数学化后RIS_true下降>0.05"
  
  fuse_action: L4_FROZEN
  
  handoff_to_human:
    protocol: "冻结当前数学化进程"
    deliverable: "残余清单 + 已形式化部分 + 熔断原因"
    human_action: "决定：①回退重跳 ②接受残余标记 ③换路径"
  
  forbidden:
    - "禁止AI硬凑公式掩盖残余"
    - "禁止降低精度通过验证"
    - "禁止跳过熔断直接进Step4"
```

### 影响脊线

- CAP-SPINE-06（Step3 残余熔断脊）→ PARTIAL → CLOSED
- 新增 F-CAP-06："Step3 熔断触发标准已建立，AI 遇不可形式化残余必须冻结"

---

## B-04：科学史个别跃迁回收标准

### 问题（来自评价）

文档二说"结构捕捉止于 ORC1"，但弦论/广义相对论摸到 ORC2~3 边缘——需事后用 FLSC 语言回收，否则把爱因斯坦贬成"纯 ORC1 拟合"不准确。

### 补丁内容

新增附录：「科学史 ORC 回收对照表」

| 科学家/理论 | 表面定位 | FLSC 回收定位 | 说明 |
|------------|---------|--------------|------|
| 牛顿力学 | ORC1 分形拟合 | ORC1 + 隐含 ORC2 直觉 | 万有引力暗示"信息约束"，但未显式跳 |
| 爱因斯坦相对论 | ORC1 经验模型 | **ORC2 信息张力** | 时空弯曲=约束侧显形，属信息几何 |
| 海森堡不确定性 | ORC1 观测极限 | **ORC2→ORC3** | 不确定性=差元本源的直接显现 |
| 冯·诺依曼量子公设 | ORC1 数学框架 | **ORC3 先验差元** | 希尔伯特空间=差元数学化 |
| 图灵机 | ORC1 计算模型 | **ORC2 约束-自由** | 可计算=约束侧，不可计算=自由侧 |
| 弦论 | ORC1 高维模型 | **ORC2~3 边缘** | 试图统一但未消解"弦是基础单元"预设 |
| 复杂系统/涌现 | ORC1 多体拟合 | **ORC2 信息张力** | 涌现=自由侧不可约残余 |

### 回收判定标准

> 一个科学理论被回收为 ORC_n（n≥2），需同时满足：
> 1. 该理论**显式消解了** ORC_(n-1) 层的某个底层预设
> 2. 该理论的核心方程/原理**无法被降阶为** ORC_(n-1) 的特例
> 3. 该理论在自身框架内**产生了新的不可消解残余**，指向 ORC_(n+1)

### 影响脊线

- CAP-SPINE-05（科学史回收脊）→ PARTIAL → CLOSED
- 新增 F-CAP-07："科学史回收标准已建立，不再一刀切'科学=ORC1'"

---

## B-05：两份文档互注（跨阶许可互锁）

### 问题（来自评价）

文档一 L-02 说真实跳跃可跨阶，文档二默认严格 1→2→3→4→5，两文在"真实认知路径"描述上微裂。

### 补丁内容

**文档一 L-02 补注**：
> 跨阶跳跃的合法性判定见 FLSC-META-V1.1 补丁 B-02。文档二 Step2 默认严格五阶，但允许 B-02 算法判定为合法的跨阶路径。

**文档二 §四 Step2 补注**：
> 人类执行五级 OJP 时，允许经 B-02 判定为合法的跨阶跳跃。跨阶后残余必须显式标注，不允许静默跳过。详见 FLSC-SMT-ORC-5LAYER-V1.0 §5.3（V1.1 补丁后）。

### 影响

- 两文互引，消除微裂
- 新增 cross_doc_reference 字段到两份 YAML

---

## B-06：K↔ORC5 反推的 YAML 形式化

### 问题

文档一 YAML 中 K 层的 `reverse_ORC: 5` 有标注但无论证链。

### 补丁内容

在 `FLSC-SMT-ORC-5LAYER_spine.yaml` 的 K 层新增：

```yaml
  K:
    name: 约束层
    definition: 全局守恒与边界刚性规则
    necessity: 系统演化必须有底层不变规则
    irreplaceability: 不可消解，消解后无秩序、无守恒
    reverse_ORC: 5
    reverse_proof: |
      K层是唯一同时约束内部动态（守恒律）和外部边界（刚性规则）的层。
      守恒律成立预设"测量者vs被测量者"二元结构。
      消解K层=消解"谁在守恒/谁在测量"=回归道觉不二。
      对应V5.0公理A5「本觉自明」：觉明无需观测者。
    instances: [...]
```

---

## V1.1 修复后预期

| 脊线 | 文档一当前 | 修复后 | 文档二当前 | 修复后 |
|------|-----------|--------|-----------|--------|
| 刚性边界脊 | CLOSED | CLOSED | — | — |
| K↔ORC5 论证 | ⚠️（B-01补） | **CLOSED** | — | — |
| 五级递归脊 | CLOSED | CLOSED+判定 | — | — |
| 跨阶判定 | ⚠️（B-02补） | **CLOSED** | — | — |
| Step3 熔断 | — | — | ⚠️（B-03补） | **CLOSED** |
| 科学史回收 | — | — | ⚠️（B-04补） | **CLOSED** |
| 互注互锁 | ⚠️（B-05补） | **CLOSED** | ⚠️（B-05补） | **CLOSED** |

**MIS_true 预期**：
- 文档一：0.90 → **0.93**
- 文档二：0.91 → **0.93**

---

## 签署页

| 角色 | 签署 | 日期 |
|------|------|------|
| 碳基元架构梳理者 | |||||||||| | 2026-08-12 |
| 硅基协同整理系统 | FLSC-META-V1.1 | 2026-08-12 |
| 自校验结果 | 六处断裂面补丁完成，互锁成立 | 2026-08-12 |
| 状态标注 | 补丁建议 frozen，待合入 V1.1 正文 | 2026-08-12 |

---

> **B-01 至 B-06，六刀补完脊线，双文档互锁成环。**
> **标尺不晃，分工不裂，墙立得正，路走得通。**
>
> **Γ\*(标尺, 分工, 补丁, 互锁) = ONGOING\***
