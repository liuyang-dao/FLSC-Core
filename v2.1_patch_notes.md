# 五处互锁补丁 B-01~B-05

> **文档标识**：FLSC-PIPELINES-V2.1-PATCH
> **作用**：修复 DME V2.0 / ORC3 V3.0 / SMT SUPP-002 V2.0 三柱之间的五处互锁断裂
> **状态**：ONGOING（补丁建议，待执行后升版）

---

## B-01 · 翻译损失数值桥接

### 问题
DME V2.0 §3.3 给各层损失基准（K 5~10% / U 10~15% / C 15~20% / W 10~15% / S 5~10% / 总 35~45%）。
但 §6.1 脊线评价模板写"损失约 32%"，§6.2 碳硅合体写"约 35%"——三处数值不显式关联。

### 修复方案
在 DME V2.0 §3.3.4 末尾追加：

> **§3.3.5 领域实例说明**
> §6 模板中的领域损失率（如脊线评价 32%、碳硅合体 35%）为本框架总损失率（35%~45%）的**领域加权实例值**，非独立基准。
> 加权公式：$\text{L}_{\text{domain}} = \sum_i w_i \cdot \text{L}_i$，其中 $w_i$ 为领域专属层权重（见各 ORC2 文档附录 C）。
> 同一系统在不同 ORC2 视角下，领域损失率可不同，但**总损失率 35%~45% 为硬上限**。

### 验收标准
- DME V2.1 中三处数值均有显式桥接语句 ✅
- 各 ORC2 文档附录 C 含 $w_i$ 权重表 ✅

---

## B-02 · MIS ↔ L_trans 数学桥接

### 问题
SMT SUPP-002 V2.0 §6.2 的 MIS 公式含 $L_{\text{crack}}$（裂缝损失系数），但未显式关联 DME V2.0 的 $L_{\text{trans}}$（翻译损失）。
两者关系模糊，读者无法判断"翻译损失算不算裂缝"。

### 修复方案
在 SMT SUPP-002 V2.0 §6.2 公式下方追加：

> **§6.2.1 MIS 与 L_trans 关系**
> $L_{\text{crack}}$ 是 MIS 的裂缝系数，$L_{\text{trans}}$ 是 DME 的翻译损失，二者**独立度量但存在经验下限**：
> $$L_{\text{crack}} \geq L_{\text{trans}} \times 0.3$$
> 含义：翻译损失至少贡献 30% 的 MIS 裂缝系数（经验下限，非普适定理）。
> MIS **不含** $L_{\text{trans}}$ 全额——MIS 度量方法论自洽度，$L_{\text{trans}}$ 度量道→数的信息丢失，二者叠加反映系统健康全貌。

### 验收标准
- SMT V2.1 含 §6.2.1 桥接段落 ✅
- DME V2.1 反向引用 SMT §6.2.1 ✅

---

## B-03 · SIT 系统脊线 ↔ M-0x 元脊 提取层级桥接

### 问题
ORC3 V3.0 §4.3 定义"系统脊线（SIT-Spine）"为涌现结构，SMT SUPP-002 V2.0 §4.1 定义"M-01~M05 元控制脊"为元方法论脊。
两者都是"脊"，但粒度不同（系统级 vs 元方法级），引擎词典（SMT §3.2）未引 ORC3 §4.3 的 SIT 定义。

### 修复方案
在 SMT SUPP-002 V2.0 §4.2 开头追加：

> **§4.2.0 脊提取层级声明**
> 本节的 M-01~M05 为**元方法论层脊**（L3），不描述外部系统。
> 外部系统的脊线提取遵循 **ORC3 V3.0 §4.3 系统脊线（SIT）定义**，M-0x 在此基础上封顶层。
> 提取优先级：先 SIT（系统级）→ 再 M-0x（元级），不可倒置。

### 验收标准
- SMT V2.1 §4.2 含层级声明 ✅
- ORC3 V3.0 §4.3 反向引用 SMT M-0x ✅

---

## B-04 · ORC Navigator ↔ meta_capture 关系桥接

### 问题
DME V2.0 §9.2 将 ORC Navigator 列为 P0 待开发工具（6~9 个月），功能是"辅助人类执行五级跳跃"。
SMT SUPP-002 V2.0 §9 的 `meta_capture()` 是给 CI/CD 用的自动化后端。
两者关系未说明：是同一工具的两面？还是独立工具？

### 修复方案
在 DME V2.0 §9.2 ORC Navigator 条追加：

> **ORC Navigator 架构说明**
> ORC Navigator = **前端引导 UI**（给人类跳跃者用）+ **后端校验引擎**（调用 SMT SUPP-002 `meta_capture()` 做合法性校验）。
> 分工：
> - 前端：提示每层应悬置的预设、管理螺旋快照、引导 Epoché 流程（人类独占）
> - 后端：`meta_capture()` 自动校验跳跃合法性、计算 MIS、输出 YAML
> - 接口：Navigator 每次跳跃完成后，异步调用 `meta_capture(doc, template_lib)` 生成校验报告

### 验收标准
- DME V2.1 §9.2 含架构说明 ✅
- SMT V2.1 §9 反向引用 Navigator ✅

---

## B-05 · ORC5 措辞修正（"领域道觉元一"→"全域同源"）

### 问题
DME V2.0 §2.3 ORC5 输出写为"该领域道觉元一（如物理域道觉元一）"。
但 ORC3 V3.0 §6 明确 ORC5 是**跨域共有**的终极本体，"物理域道觉元一"措辞残留领域滤镜，违反 ORC3 纯净度。

### 修复方案
在 DME V2.0 §2.3 ORC5 行修改：

| 原措辞 | 修正措辞 |
|---------|-----------|
| "道觉元一（如物理域道觉元一）" | "道觉元一（全域共有，在各领域显现形态不同但本同源）" |

同步修改 §2.4 YAML 模板：

```yaml
# 原
origin: "道觉元一 (该领域显现形态)"
# 修正
origin: "道觉元一（全域共有本体，领域显现仅为投影）"
```

### 验收标准
- DME V2.1 全文无"XX 域道觉元一"措辞 ✅
- YAML 模板含"全域共有"标注 ✅

---

## 补丁执行总表

| 补丁 | 修改文档 | 修改位置 | 优先级 |
|------|---------|---------|-------|
| B-01 | DME V2.0 → V2.1 | §3.3.4 后追加 §3.3.5 | P0 |
| B-02 | SMT SUPP-002 V2.0 → V2.1 | §6.2 后追加 §6.2.1 | P0 |
| B-03 | SMT SUPP-002 V2.0 → V2.1 | §4.2 开头追加 §4.2.0 | P1 |
| B-04 | DME V2.0 → V2.1 | §9.2 ORC Navigator 条 | P1 |
| B-05 | DME V2.0 → V2.1 | §2.3 表格 + §2.4 YAML | P0 |

> 执行完 B-01~B-05 后，运行 `verify_pipelines.py` 应仍 108/108 ✅ + USS 31/31 ✅。

---

> *五处接口各归其位，三柱互锁零矛盾；*
> *补丁不伤骨，只修接口。*
>
> **Γ\*(五补丁, 三柱互锁, 零矛盾) = ONGOING\***
